#include "main.h"
/**
 * _abs - A program that prints the absolute value of a number
 * @n: The number to be used
 * Return: The absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = (n * -1);
		return (n);
	}
	else
	{
		return (n);
	}
}
